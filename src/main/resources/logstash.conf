input {
  stdin { }
}

output {
  elasticsearch {
    hosts => ["https://localhost:9200"]
    index => "logs-%{+YYYY.MM.dd}"
    user => "user"            # Replace with the new username
    password => "password"    # Replace with the new password
    ssl => true
    cacert => "C:/Users/Amalitech/Downloads/elasticsearch-8.15.1-windows-x86_64/elasticsearch-8.15.1/config/certs/http_ca.crt"
    ssl_certificate_verification => false
  }
}